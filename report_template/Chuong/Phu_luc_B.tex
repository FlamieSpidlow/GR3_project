\documentclass[../DoAn.tex]{subfiles}
\begin{document}
Phụ lục này dùng để bổ sung bảng test case đầy đủ, ảnh giao diện phụ hoặc hướng dẫn chạy local nếu cần. Các nội dung này chỉ nên đưa vào sau khi có dữ liệu kiểm thử, ảnh giao diện và cấu hình môi trường được xác nhận.
\end{document}
